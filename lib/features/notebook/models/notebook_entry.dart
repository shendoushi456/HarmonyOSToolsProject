/// 记事本实体，对齐 Android NotebookBean。
class NotebookEntry {
  final String id;
  final String content;
  final String time;

  const NotebookEntry(
      {required this.id, required this.content, required this.time});
}
